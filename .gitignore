# Environment variables
.env
.env.local
.env.*.local

# Python Virtual Environment
.venv/
venv/
*.pyc
__pycache__/
*.pyo
*.pyd
.Python
env/
pip-log.txt
pip-delete-this-directory.txt
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.log
.mypy_cache
.pytest_cache
.hypothesis

# Poetry
poetry.lock

# Streamlit
.streamlit/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code (local-only overrides; shared commands/skills are committed)
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# Docker
docker-compose.override.yml
.docker/

# Spark
infrastructure/docker/spark/data/
infrastructure/docker/spark/logs/
infrastructure/docker/spark/events/
infrastructure/docker/spark/work/
infrastructure/docker/spark/jars/*.jar
!infrastructure/docker/spark/jars/.gitkeep

# Jupyter
data-platform/streaming/spark/notebooks/.ipynb_checkpoints/
*.ipynb_checkpoints

# Kafka/Debezium data
data/
logs/
*.log.*

# Database dumps
*.dump
*_dump.sql
backup_*.sql
export_*.sql

# Temporary files
tmp/
temp/
.tmp/

# Application specific
data-platform/streaming/spark/checkpoints/
data-platform/streaming/spark/output/

# Volume mounts (runtime data)
volumes/
## Terraform
infrastructure/terraform/.terraform/
infrastructure/terraform/.terraform.lock.hcl
infrastructure/terraform/terraform.tfstate
infrastructure/terraform/terraform.tfstate.backup
infrastructure/terraform/crash.log
infrastructure/terraform/.terraform.tfstate.*
infrastructure/terraform/terraform.tfvars

# jmx-exporter jar fetched by scripts/setup_observability.sh; not committed.
infrastructure/docker/jmx-exporter/*.jar

# GX Data Docs runtime output (see openspec/changes/add-gx-data-docs). The
# validations store, data-docs HTML, and Prometheus textfile are all
# regenerable from each batch; only the config + suite definitions are
# committed.
data-platform/governance/gx-runtime/uncommitted/
data-platform/governance/gx-runtime/checkpoints/
data-platform/governance/gx-runtime/textfile/gx.prom
# GX auto-scaffolds these on first `context.get_context(...)` — they carry
# no committed value (per-deployment UUID, empty plugin dirs).
data-platform/governance/gx-runtime/expectations/.ge_store_backend_id
data-platform/governance/gx-runtime/plugins/
